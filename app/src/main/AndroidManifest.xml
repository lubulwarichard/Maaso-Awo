<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="net.unique.awo">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
            android:name=".App"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".ui.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <action android:name="android.intent.action.VIEW"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
                android:name=".ui.login.UserLogin"
                android:label="@string/enter_phone_number_activity_name">
        </activity>
        <activity android:name=".ui.home.MainActivity">
        </activity>
        <activity
                android:name=".ui.login.VerifyPhoneSMS"
                android:label="@string/verify_phone_number_activity_name">
        </activity>
        <activity android:name=".ui.home.Main2Activity">
        </activity>
        <activity android:name=".ui.checkout.CheckoutActivity" />
    </application>

</manifest>